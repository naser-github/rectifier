/**
 * Human-readable labels for result formats.
 */
export const FORMAT_LABELS: Record<string, string> = {
  json: "JSON",
  yaml: "YAML",
  xml: "XML",
  csv: "CSV",
};

/**
 * File extensions for result formats.
 */
export const FORMAT_EXTENSIONS: Record<string, string> = {
  json: ".json",
  yaml: ".yaml",
  xml: ".xml",
  csv: ".csv",
};
